매수 주문이 체결된 이후, 시장가로 즉시 던지는 것이 아니라 **'내가 산 가격보다 정확히 1% 낮은 가격'에 미리 지정가(Limit Order) 매도 주문을 대기**시켜 놓는 방식으로 수정해 드리겠습니다.

이 방식은 실시간으로 주가를 감시하며 대기할 필요 없이, **매수 체결이 확인되는 즉시 매도 벽을 세워두는 구조**이기 때문에 프로그램 부하가 적고 시스템이 훨씬 안정적이라는 장점이 있습니다.

---

## 1. 지정가 손절매 수정 코드 (Python)

키움 API의 실시간 체결 데이터(`OnReceiveChejanData`) 중, 주문이 실제로 체결되었을 때 발생하는 **`gubun == '0'`(주문/체결 통보)** 내부의 **'체결구분: 체결'** 상태를 포착하여 연동하는 방식입니다.

```python
import sys
from PyQt5.QtWidgets import *
from PyQt5.QAxContainer import *
from PyQt5.QtCore import *

class KiwoomLimitStopLoss(QAxWidget):
    def __init__(self):
        super().__init__()
        self.setControl("KHOPENAPI.KHOpenAPICtrl.1")
        
        # 이벤트 슬롯 연결
        self.OnEventConnect.connect(self._login_slot)
        self.OnReceiveChejanData.connect(self._receive_chejan_data) # 실시간 체결/잔고 이벤트
        
        self.login_event_loop = QEventLoop()
        self.account_number = ""
        
        # 중복 주문 방지를 위해 이미 지정가 매도를 걸어둔 주문 번호나 종목 보관
        self.processed_orders = set()

    def comm_connect(self):
        self.dynamicCall("CommConnect()")
        self.login_event_loop.exec_()

    def _login_slot(self, err_code):
        if err_code == 0:
            account_list = self.dynamicCall("GetLoginInfo(QString)", "ACCNO")
            self.account_number = account_list.split(';')[0]
            print(f"로그인 성공 | 계좌번호: {self.account_number}")
        self.login_event_loop.exit()

    # [수정된 핵심 로직] 실시간 체결 통보 이벤트
    def _receive_chejan_data(self, gubun, item_cnt, fid_list):
        """
        gubun: '0'은 주문/체결 통보 (매수 완료 순간을 포착)
        """
        if gubun == '0':
            # 1. 체결 상태 확인 (체결구분: '체결'인 것만 파싱, '접수'나 '취소'는 제외)
            chejan_status = self.dynamicCall("GetChejanData(int)", 913).strip() 
            order_status = self.dynamicCall("GetChejanData(int)", 91).strip() # 주문상태 (체결 등)
            
            # 2. 매수 주문이 체결 완료되었는지 확인
            # (905) 주문구분: '+매수' 또는 '매수'
            order_gubun = self.dynamicCall("GetChejanData(int)", 905).strip()
            
            if "매수" in order_gubun and (chejan_status == "체결" or order_status == "체결"):
                order_no = self.dynamicCall("GetChejanData(int)", 926).strip()     # 원래 매수 주문번호
                
                # 이미 매도 걸어둔 매수체결 건이면 패스 (중복 실행 방지)
                if order_no in self.processed_orders:
                    return
                
                code = self.dynamicCall("GetChejanData(int)", 9001).strip()[1:]    # 종목코드
                item_name = self.dynamicCall("GetChejanData(int)", 302).strip()     # 종목명
                che_price = int(self.dynamicCall("GetChejanData(int)", 910))       # 체결가 (내가 산 가격)
                che_qty = int(self.dynamicCall("GetChejanData(int)", 911))         # 체결수량

                print(f"✨ [매수 체결 완료] {item_name}({code}) | 수량: {che_qty}주 | 체결가: {che_price}원")

                # 3. 1% 하락된 손절 가격 계산 및 호가단위 맞춤
                # 주식은 원단위가 정해져 있으므로 정수형(int) 변환
                limit_sell_price = int(che_price * 0.99) 
                limit_sell_price = self._align_hoga(limit_sell_price, code) # 호가단위 보정

                print(f"🛑 [지정가 매도 예약] {limit_sell_price}원에 {che_qty}주 지정가 걸어둡니다.")
                
                # 작업 완료 목록에 등록
                self.processed_orders.add(order_no)
                
                # 4. 즉시 지정가 매도 주문 전송
                self.send_limit_sell_order(code, che_qty, limit_sell_price)

    # 지정가 매도 주문 함수
    def send_limit_sell_order(self, code, quantity, price):
        # 2: 신규매도, "00": 지정가 (원하는 손절가격을 매개변수로 전달)
        res = self.dynamicCall(
            "SendOrder(QString, QString, QString, int, QString, int, int, QString, QString)",
            ["지정가손절매도", "4000", self.account_number, 2, code, quantity, price, "00", ""]
        )
        if res == 0:
            print(f"▶ [{code}] {quantity}주 -> {price}원 지정가 매도벽 설정 성공")
        else:
            print(f"❌ 지정가 매도 주문 실패 (에러코드: {res})")

    # [보완] 국내 주식 호가 단위에 맞게 가격을 보정해주는 유틸리티 함수
    def _align_hoga(self, price, code):
        # 코스피/코스닥 및 가격대별 호가 단위가 다르나 기본 대형주/KOSPI 기준으로 단순화 예시
        # 정확히 하려면 키움 호가 단위를 체크해야 하지만 보통 하락 가격 이하의 가장 가까운 호가로 내림 처리
        if price < 2000: return price  # 2천원 미만은 1원 단위
        elif price < 5000: return (price // 5) * 5  # 5원 단위
        elif price < 20000: return (price // 10) * 10  # 10원 단위
        elif price < 50000: return (price // 50) * 50  # 50원 단위
        elif price < 200000: return (price // 100) * 100  # 100원 단위
        elif price < 500000: return (price // 500) * 500  # 500원 단위
        else: return (price // 1000) * 1000  # 1,000원 단위

```

---

## 2. 변경된 매매 시나리오

1. **사용자/프로그램의 매수**: 사용자가 임의로 혹은 프로그램이 어떤 종목을 매수합니다.
2. **매수 체결 순간 catch**: `gubun == '0'` 이벤트가 작동하면서 방금 매수한 종목의 **실제 체결 가격**을 실시간으로 읽어옵니다.
3. **-1% 가격 계산**: `체결가 * 0.99` 계산을 거쳐 매도 목표가를 산출합니다. (예: 50,000원에 샀다면 49,500원)
4. **지정가 매도 주문 자동 제출**: 호가 구분 번호를 `"00"`(지정가)로 셋팅하고 계산된 가격으로 즉시 매도 주문을 넣습니다. 이제 내 주식은 호가창의 -1% 자리에 매도 물량으로 대기하게 됩니다.

---

## 💡 구현 시 꼭 알아두셔야 할 점

* **호가 단위 불일치 오류 방지**: 계산기 상으로 $50,000 \times 0.99 = 49,500$원 처럼 딱 떨어지면 문제가 없으나, 만약 계산된 가격이 `49,532원` 같은 식으로 나오면 키움 서버에서 '호가 단위 불일치 에러'를 뱉으며 주문을 거절합니다. 이를 방지하기 위해 상기 코드에 `_align_hoga`라는 단위 절사 함수를 포함해 두었습니다.
* **미체결 상태의 리스크**: 지정가 매도 주문을 1% 아래에 대기시켜 두더라도, 장중에 주가가 갑자기 폭락하여 내 매도 호가(예: 49,500원)를 **순간적으로 갭(Gap)하락으로 건너뛰고** 더 아래로 추락해 버리면 내 주문은 체결되지 않은 채 공중에 떠 있을 수 있습니다. (이 때문에 완벽한 자산 보호를 위해서는 보통 '시장가 조건부 매도'를 쓰거나, 시스템이 주기적으로 미체결 잔량을 감시하게 만듭니다.)
